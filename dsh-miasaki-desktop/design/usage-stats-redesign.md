# 「用量」页重构与全局用量统计入口 · 规划设计（token-monitor v0.4.0 预研）

> 状态：**定稿（v0.4.0 已落地，2026-09-06）**。本文记录 2026-09 会话提出的新设计：会话「用量」Tab
> 只统计当前会话（与「轨迹」页同源同口径），页面精简；跨会话总量统计（热力图 /
> 使用趋势 / 使用分布 / 使用总量）整体迁出，统一收口到左侧边栏设置按钮旁的
> 「用量统计」入口（浮窗）。四个决策点已拍板并实现：D1 浮窗 / D2 日限额迁全局页 /
> D3 分布首版模型 + 会话 Top N / D4 会话页加「会话活跃时长」卡。实现细节与验证
> 记录见 `design/CHANGELOG.md` 2026-09-06 条目。

---

## 1. 背景与目标

用户对现有「用量」Tab（`dsh-token-monitor` v0.3.3，`conversation.view` order 15）提出两条诉求：

1. **会话页归会话**：每个会话的用量页只统计**当前会话**的用量，统计方法与「轨迹」页一致
   —— 从当前会话自己的数据投影，不掺入其他会话、不掺入跨会话累计。
2. **总量收口到全局**：该精简的精简；统计总量的模块（热力图、使用趋势、使用分布、
   使用总量）统一放到左侧边栏**设置按钮旁新增的「用量统计」按钮**，点击进入
   **新页面或浮窗**，显示 DSH 的使用总量。

设计目标（按优先级）：

- G1：会话「用量」Tab 内**不存在任何跨会话数据**，口径清晰、页面短小；
- G2：全局总量统计有唯一入口（侧栏脚部「用量统计」），与「设置」并列但不混入设置语义；
- G3：现有账本数据（`usage-log.jsonl`，380 天）零迁移成本，历史统计不丢；
- G4：保持零新依赖（纯 React / 手写 SVG / CSS grid）、主题令牌化、与 DSH 升级兼容。

---

## 2. 现状盘点

### 2.1 数据三通道（host 半 `lib/index.js`）

| 通道 | 来源 | 会话范围 | 生命周期 | 现状用途 |
|---|---|---|---|---|
| 官方持久聚合 | `sessionProjections.snapshot`（tokenUsage / contextPressure / contextBreakdown / sessionStats）+ `tokenMeter.measure` | **单会话** | 全会话日志、跨重启 | 会话页各卡（与「轨迹」页同源） |
| 实时明细 | `llm/stream` 增量 + `tools/result` | 按 `sessionId` 键控 | 本进程启动起 | 按模型明细 / 工具徽章 |
| 跨会话账本 | 与实时明细同口径按日累计，落盘 `usage-log.jsonl` | **跨会话** | 插件部署起、跨重启、380 天 | 总览五卡 / 热力图 / 趋势 / 占比 / 今日限额 |

### 2.2 现有 Tab 区块（client 半 `lib/client.js`）及去向判定

| 现区块 | 数据源 | 精简后的去向 |
|---|---|---|
| 总览五卡（累计 / 峰值 / 最长聊天 / 连续天数） | 账本 | **迁全局页** |
| Token 活动年热力图（日 / 周 / 累计） | 账本 heatmap | **迁全局页** |
| 时间范围（近 7/30 日）+ 每日趋势图 + 模型用量环形图 | 账本 trend | **迁全局页** |
| 上下文剩余 hero（剩余 % + 分段条） | 官方（本会话） | **保留**（会话页头牌） |
| 今日用量 · 全部会话 + 日限额 + 重置账本 | 账本 + 本地配置 | **迁全局页**（重置与限额设置一并迁走） |
| 统计卡组（模型输出 / 缓存读写 / 官方累计 / 轮次步数） | 官方（本会话） | **保留**（并入会话总览） |
| 性能小卡（TTFT / 解码 / 模型与工具耗时） | 官方（本会话） | **保留** |
| 按模型明细条形 + 工具调用徽章 | 实时 | **保留但按会话过滤**（见 2.3） |
| 三通道口径脚注 | — | 拆分：会话页只写会话口径，全局页写账本口径 |

### 2.3 现存的会话边界缺陷（本次一并修正）

- `buildSummary(sessionId)` 把 `live.calls` / `live.tools` **全量返回**（未按
  `sessionId` 过滤），会话页的按模型明细与工具徽章实际混入了**同进程其他会话**的
  数据——与「只统计当前会话」的目标直接冲突。重构后 host 按 `sessionId` 过滤后再下发。
- `summary` 载荷恒定携带 30 天账本趋势 + 总览统计 + 限额配置，会话页每 3 秒重复拉取
  与自身无关的全局数据。重构后拆分路由，会话页载荷显著变小。

---

## 3. 目标信息架构

```
左侧边栏脚部
  ├─ 设置（DSH 原生）
  └─ 用量统计（新增，sidebar.footer.action）──点击──▶ shell.overlay 浮窗
                                                     └─ 全局用量统计页
会话视图（对话 / 轨迹 / 用量）
  └─ 用量 Tab（精简为纯会话视角）
```

两个视图职责一刀切：**会话内的一切 → 用量 Tab；跨会话的一切 → 全局浮窗**。
全局浮窗是 root 作用域、与当前会话解耦——打开期间切换会话浮窗不受影响，这也是它
相对「塞进某个会话 Tab」的结构性优势。

---

## 4. 会话「用量」Tab 精简设计

### 4.1 区块清单（自上而下）

1. **上下文剩余 hero**（保留原样）：大字号剩余 % + 全宽分段条（分母 = contextWindow，
   45/75/95 三档变色）+ 系统/工具/消息/未使用图例。本会话的"剩余视角"头牌。
2. **会话用量总览**（由原统计卡组重组）：本会话累计 Tokens（官方口径
   `tokenUsage` 四段 + `tokenMeter` 总口径）、输入 / 输出 / 缓存读 / 缓存写分项、
   轮次 / 步数、会话活跃时长（可选，见 §10 决策点 D4）。
3. **按模型明细**（保留）：实时采集、**仅当前会话**；四段条形（输入 / 输出 / 缓存读 /
   推理）+ 调用次数。
4. **工具调用**（保留）：本会话内工具 × 次数徽章。
5. **性能**（保留）：TTFT / 解码耗时 / 解码速度 / 模型耗时 / 工具耗时（官方）。
6. **口径脚注**（改写）：官方聚合覆盖本会话全程（含插件启用前历史、跨重启，与
   「轨迹」页同源）；按模型明细与工具计数为插件实时采集、仅本进程；**本页不含任何
   跨会话累计**。全局统计请见左侧边栏「用量统计」。

### 4.2 数据面（会话路由）

- 新路由 `GET /dsh-token-monitor/session?sessionId=…`：仅返回
  `{ sessionId, official, live(按会话过滤), sampledAt }`，3 秒轮询，载荷瘦身。
- 旧 `/summary` 不再承担会话页职责（其全局部分并入 `/global`，见 §6.1）。

### 4.3 明确的"删除项"

总览五卡、热力图、时间范围 + 趋势图、模型环形图、今日用量 · 全部会话、日限额设置、
重置账本按钮——全部从 Tab 移除（迁往全局浮窗）。Tab 保留现有
`:has(.tokmn-pane)` 列宽解耦 CSS 不动。

---

## 5. 全局「用量统计」入口设计

### 5.1 槽位选型（已按当前 GUI 实况槽位树核实）

| 方案 | 槽位 | 形态 | 评估 |
|---|---|---|---|
| **A（推荐）浮窗** | 按钮：`sidebar.footer.action`（设置旁脚部动作位）；内容：`shell.overlay`（全帧浮层，位于所有列之上、滚动容器之外） | 点击开全屏浮层，Esc / 关闭按钮 / 点背板收起 | 图表页需要横向空间（52 周热力图、多序列趋势图），浮层全帧最宽裕；与"设置"的配置语义分离；符合用户"新页面或者浮窗"的后者 |
| B 设置页 | `settings.section`（每个 list 项一个设置页） | 设置抽屉内的新页 | 入口天然、零浮层代码；但设置面板为窄抽屉，图表局促，且"用量统计"是**查看**而非**配置**，语义不符；作为兜底方案保留 |
| C 独立路由页 | 无现成页面槽位，需自造 | 新页面 | 偏离 DSH 单页结构，成本最高，不推荐 |

**定稿建议：方案 A**。按钮注册 `sidebar.footer.action`（id `usage-stats`，label
「用量统计」，图标 + 文字；收起态侧栏仅显图标、悬浮提示，accessible name 由内容提供
——具体渲染行为在实现期以目检核实，与 `settings.trigger` 同法）。

### 5.2 浮窗生命周期（client 半）

- 按钮与浮层同为插件自注册的两个槽位条目，开合状态经插件内小 store 共享
  （模块级 `useSyncExternalStore` 或等价极简方案，不引入依赖）；
- 浮层条目渲染：关闭态返回 `null`，开启态返回全屏背板 + 内容面板；
- 关闭途径：右上关闭按钮、Esc 键、点击背板空白处；
- 开启期间挂起全局数据轮询，关闭即停（不空转）。

---

## 6. 全局用量统计页模块设计（浮窗内容）

顺序即用户点名的模块序：**总览 → 热力图 → 使用趋势 → 使用分布 → 使用总量**。

1. **头部**：标题「用量统计」+ 记账起止说明（"自 YYYY-MM-DD 起记录 · 保留 380 天"）
   + 关闭按钮。
2. **使用总量 · 总览卡组**（原总览五卡 + 活跃天数，ZCode 头部统计行同构）：
   累计 Token 数 / 单日峰值 / 活跃天数 / 最长聊天时长 / 当前连续天数 / 最长连续天数。
3. **热力图**（原「Token 活动」整块迁移）：年视图 52 周、周一对齐、月标签；
   每日 / 每周 / 累计三态；悬浮富提示（日期 + tokens + 轮消息）。
4. **使用趋势**（原「时间范围 + 每日趋势」迁移）：近 7 / 30 日切换；按模型多序列
   平滑曲线 + 图例显隐 + 悬浮十字明细；配色按 30 天总量排名分配。
5. **使用分布**：
   - **模型用量环形图**（原迁移）：中心范围总量 + 右侧模型列表；
   - **会话用量 Top N**（**新增**）：近 30 日按会话聚合排行（tokens + 轮消息 +
     占比条），数据由账本 `sessionId` 维度聚合（§6.1）；会话标题尽力解析
     （`sessions.get` 取标题，失败降级显示 sessionId 截断）；
   - （后续可选）按星期 / 时段分布——账本已有日期与时间戳，v1.1+ 再议。
6. **使用总量 · 今日与限额**（原「今日用量 · 全部会话」+ 日限额设置迁移）：
   今日累计大数字 + 输入 / 输出 / 缓存读 / 轮消息分项；日限额进度条（未设置可就地
   设置，K/M 单位；已设置可编辑 / 清除）；**「重置账本」按钮迁至此**（confirm
   确认后清空，热力图 / 趋势 / 占比即时归零重计）。
7. **口径脚注**：全局统计来自跨会话账本（`usage-log.jsonl`，380 天，自插件首次部署
   起累计、跨 host 重启持久；部署前的历史会话不在其中）；限额为本地自定义配置
   （DSH 无配额接口）。

### 6.1 数据面（全局路由）

- 新路由 `GET /dsh-token-monitor/global`：`{ stats, today, trend(30d, 含按模型明细),
  sessions(近 30 日 Top N，新增聚合), since, config }`，浮窗开启期间 5 秒轮询；
- `GET /dsh-token-monitor/heatmap` 保持原样，浮窗开启期间 60 秒轮询；
- `GET|POST /dsh-token-monitor/config`、`POST /dsh-token-monitor/reset` 不变；
- 账本写入路径（`llm/stream` 双写 + 5s 节流 flush + 退出兜底）**完全不动**——
  全局页与现热力图同源，历史数据零迁移。

---

## 7. 客户端实现要点

- 仍为单插件 `dsh-token-monitor`（同一 host 账本，避免拆插件带来双写 / 双账本风险），
  client 半在 `conversation.view` 之外新增两个 root 作用域注册：
  `sidebar.footer.action`（按钮）与 `shell.overlay`（浮层）；
- 会话页与全局页的图表组件（热力图 / 趋势 / 环形图 / Seg 分段切换 / 悬浮提示 /
  格式化函数）**原样复用**，只是挂载点从 Tab 换到浮层；
- 无 JSX、无新依赖，主题令牌 `--dsw-alias-*` 不变；浮层背板用 `--tokmn-*` 自派生
  中性色同法处理明暗主题。

---

## 8. 兼容与迁移

- 插件版本 **v0.3.3 → v0.4.0**（behavioral 变更：路由拆分 + UI 重组）；
- `usage-log.jsonl` 与 `config.json` 格式不动，历史账本与限额配置原样保留；
- 部署不变：profile 目录 `pnpm install` + **重启 host**（web bundle 图重建）；
- 旧 `/summary` 路由退役（插件自用、无外部消费者，直接移除并在 README 记录）。

---

## 9. 已知限制（如实记录，不掩盖）

- 全局账本只覆盖**插件部署之后**的用量；部署前的历史会话不在统计内。可选未来：
  扫描 `sessions.list()` + `sessionProjections` 回填账本（成本与收益另评估）。
- 实时明细仅覆盖本进程：切到旧会话时按模型明细可能为空（官方聚合仍完整），
  与「轨迹」页的日志投影行为一致，脚注如实说明。
- 会话 Top N 排行基于账本 `sessionId`，归档 / 删除会话只显示 ID，不做标题强解析。

---

## 10. 待定决策点（需用户拍板）

- **D1 入口形态**：浮窗（方案 A，推荐）/ 设置页（方案 B）。本文按 A 设计。
- **D2 日限额位置**：迁至全局页（推荐，会话页保持纯会话口径）/ 会话页保留迷你进度条。
- **D3 分布维度首版范围**：模型 + 会话 Top N（推荐）/ 另加星期分布 / 另加时段分布。
- **D4 会话页是否加「会话活跃时长」卡**：实时口径（本进程内 first/last），
  官方聚合无墙钟跨度字段；可选加、可先不加。

---

## 11. 实施里程碑与验证点

- **M1（v0.4.0）数据面**：host 拆分 `/session`（live 按会话过滤，修复 §2.3）与
  `/global`（含 sessions Top N 聚合）；mock 测试覆盖会话过滤与全局聚合。
- **M2（v0.4.0）会话页精简**：Tab 按 §4.1 重组，删除 §4.3 区块；目检：打开多个
  会话确认 Tab 内数字互不串扰、切换会话随动。
- **M3（v0.4.0）全局浮窗**：`sidebar.footer.action` 按钮 + `shell.overlay` 浮层 +
  §6 各模块迁移；目检：收起态侧栏图标 / Esc / 背板点击 / 浮窗开启时切会话不受影响 /
  重置账本后热力图与趋势即时归零。
- **M4 文档**：插件 README、desktop README 目录树、`design/CHANGELOG.md` 增补
  v0.4.0 条目（依据 / 触摸点 / 验证点），本文档由"规划稿"改为"定稿"。

Windows 机验证统一路径：profile 目录 `pnpm install` → 重启 host → 按 M2/M3 目检清单过一遍。
