# 任务 t-0008：并发派单验证-乙（pi）

## 目标
在并发派单验证中担任乙队：用一句话确认身份（必须包含 "PONG-B" 字样），并报告你使用的 provider 配置来源。

## 交付物
- [ ] 答案直接输出在 stdout（pi -p headless）

## 约束
- 只读任务；预算：低；超时：5 分钟
- 依赖任务：无（与 t-0007 并行，互相不可见）

## 验收标准
1. pi 进程真实执行且 exit 0
2. 输出包含 "PONG-B"
3. 与 t-0007（opencode）并行运行互不干扰

## 完成后必做
1. 答案由派单器捕获
