# 任务 t-0007：并发派单验证-甲（opencode）

## 目标
在并发派单验证中担任甲队：用一句话确认身份（必须包含 "PONG-A" 字样），并报告你读取到的 manifest 中的模型配置。

## 交付物
- [ ] 答案直接输出在 stdout（opencode run headless）

## 约束
- 只读任务；预算：低；超时：5 分钟
- 依赖任务：无（与 t-0008 并行，互相不可见）

## 验收标准
1. opencode 进程真实执行且 exit 0
2. 输出包含 "PONG-A"
3. 与 t-0008（pi）并行运行互不干扰

## 完成后必做
1. 答案由派单器捕获
